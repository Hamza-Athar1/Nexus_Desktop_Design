# Vite only exposes env vars prefixed with VITE_ to the client.
# Copy this file to .env at the repo root and adjust if your backend
# runs on a different port/host.
VITE_API_URL=http://localhost:5000/api